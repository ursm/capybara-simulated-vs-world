# Tests we don't expect capybara-simulated to satisfy. Each entry is
# `{test:, reason:}`; `test:` is preferred as the RSpec
# `full_description` (stable across upstream line-number moves) but
# may also be a `./spec/...rb:LINE` location string or a Regexp
# matched against either.
#
# Last verified against Avo 4764edf17 on 2026-05-09
# (capybara-simulated 0.0.7 / Ruby 3.3.11) running
# `bin/run-avo` (= `spec/system spec/features`).
#
# ~95% mirror Avo's own `BENCH_DRIVER=simulated` baseline at the same
# SHA — properties of the simulated/cuprite divergence on Avo's
# specific stack (flatpickr + Luxon TZ db, CodeMirror layout-derived
# measurement, TipTap, tagify, Algolia autocomplete) rather than
# driver bugs we can chase one at a time.

- test:   "Create Via Belongs to with polymorphic belongs_to when belongs_to record options exceeds associations_lookup_list_limit limits select options"
  reason: "intermittent — passes on a clean baseline run, fails when other polymorphic-options specs run first; suspected factory/cleanup ordering inside Avo"

- test:   "Actions downloading files without File.open().read downloads the file and closes the modal"
  reason: "action download flow — turbo-stream POST raises server-side `nil[]` (factory ordering inside Avo) before the StreamActions.download client code runs"

- test:   "Actions downloading files with File.open().read downloads the file and closes the modal"
  reason: "action download flow — turbo-stream POST raises server-side `nil[]` (factory ordering inside Avo) before the StreamActions.download client code runs"

- test:   "Actions downloading files with File.open().read on pdf downloads the file and closes the modal"
  reason: "action download flow — turbo-stream POST raises server-side `nil[]` (factory ordering inside Avo) before the StreamActions.download client code runs"

- test:   "Actions redirects when no confirmation redirects to hey page"
  reason: "action download flow — turbo-stream POST raises server-side `nil[]` (factory ordering inside Avo) before the StreamActions.download client code runs"

- test:   "Actions close_modal closes the modal and flashes messages"
  reason: "action download flow — turbo-stream POST raises server-side `nil[]` (factory ordering inside Avo) before the StreamActions.download client code runs"

- test:   "App alerts only displays one alert on record destroy from has_many"
  reason: "general app-level UI interactions"

- test:   "App alerts destroy from index page"
  reason: "general app-level UI interactions"

- test:   "App security xss in turbo frames 1"
  reason: "general app-level UI interactions"

- test:   "App security xss in turbo frames 2"
  reason: "general app-level UI interactions"

- test:   "Associations using *_fields api attach and detach works using show and index fields api"
  reason: "Avo Stimulus / Preact UI flow not fully exercised by simulated"

- test:   "Attach with extra fields allows to pass in data for extra fields"
  reason: "Avo Stimulus / Preact UI flow not fully exercised by simulated"

- test:   "Attachment destroy destroying an attachment when validation fails after destroying attachment rolls back the transaction and keeps the attachment"
  reason: "Avo Stimulus / Preact UI flow not fully exercised by simulated"

- test:   "Attachment destroy destroying an attachment when destroying an attachment succeeds destroys the attachment and saves the record"
  reason: "Avo Stimulus / Preact UI flow not fully exercised by simulated"

- test:   "Record previews opens the preview"
  reason: "Avo Stimulus / Preact UI flow not fully exercised by simulated"

- test:   "belongs_to not searchable new without an association when filling the poly association creating a polymorphic association creates the comment"
  reason: "polymorphic association picker — Algolia autocomplete"

- test:   "belongs_to not searchable new with an association nullifying a polymorphic association with just selecting a different association empties the commentable association"
  reason: "polymorphic association picker — Algolia autocomplete"

- test:   "belongs_to not searchable new with an association nullifying a polymorphic association with just selecting a different association changes associated record"
  reason: "polymorphic association picker — Algolia autocomplete"

- test:   "CodeField without value edit change the projects custom_css code"
  reason: "CodeMirror integration — `hasBadBidiRects` measures DOM widths and crashes the editor before render; gutter / line measurement gates off real layout"

- test:   "CodeField with regular value edit has the projects custom_css label and filled code editor"
  reason: "CodeMirror integration — `hasBadBidiRects` measures DOM widths and crashes the editor before render; gutter / line measurement gates off real layout"

- test:   "CodeField with regular value edit change the projects custom_css code to another value"
  reason: "CodeMirror integration — `hasBadBidiRects` measures DOM widths and crashes the editor before render; gutter / line measurement gates off real layout"

- test:   "CodeField with regular value show displays the projects custom_css"
  reason: "CodeMirror integration — `hasBadBidiRects` measures DOM widths and crashes the editor before render; gutter / line measurement gates off real layout"

- test:   "CodeField with pretty_generated option for a JSON code field correctly formats JSON code on create / edit and displays it in a pretty way on the show page"
  reason: "CodeMirror integration — `hasBadBidiRects` measures DOM widths and crashes the editor before render; gutter / line measurement gates off real layout"

- test:   "Create Via Belongs to edit with non-searchable belongs_to successfully creates a new user and assigns it to the comment"
  reason: "belongs-to-create flow opens a dialog whose visibility cascade we do not fully match"

- test:   "Create Via Belongs to edit with polymorphic belongs_to successfully creates a new commentable and assigns it to the comment"
  reason: "belongs-to-create flow opens a dialog whose visibility cascade we do not fully match"

- test:   "Create Via Belongs to with non-searchable belongs_to successfully creates a new user and assigns it to the comment"
  reason: "belongs-to-create flow opens a dialog whose visibility cascade we do not fully match"

- test:   "Create Via Belongs to with polymorphic belongs_to successfully creates a new commentable and assigns it to the comment"
  reason: "belongs-to-create flow opens a dialog whose visibility cascade we do not fully match"

- test:   "Create Via Belongs to with models that uses prefix_id successfully creates a new course and assigns it to the course link"
  reason: "belongs-to-create flow opens a dialog whose visibility cascade we do not fully match"

- test:   "Date field on eastern zone in an eastern (positive) Timezone edit keeps the same value on save"
  reason: "flatpickr / Luxon timezone DB — Intl.DateTimeFormat in QuickJS lacks the full IANA zone tables, so `setZone(\"Europe/Bucharest\")` falls back to UTC and the assertion sees the un-zoned datetime"

- test:   "Date field on eastern zone in an eastern (positive) Timezone edit keeps the right value on update and save"
  reason: "flatpickr / Luxon timezone DB — Intl.DateTimeFormat in QuickJS lacks the full IANA zone tables, so `setZone(\"Europe/Bucharest\")` falls back to UTC and the assertion sees the un-zoned datetime"

- test:   "Date field with relative: true index displays the date in a valid tz"
  reason: "flatpickr / Luxon timezone DB — Intl.DateTimeFormat in QuickJS lacks the full IANA zone tables, so `setZone(\"Europe/Bucharest\")` falls back to UTC and the assertion sees the un-zoned datetime"

- test:   "Date field with relative: true show displays the date in a valid tz"
  reason: "flatpickr / Luxon timezone DB — Intl.DateTimeFormat in QuickJS lacks the full IANA zone tables, so `setZone(\"Europe/Bucharest\")` falls back to UTC and the assertion sees the un-zoned datetime"

- test:   "Date field with relative: true edit when keeping the value saves the valid date"
  reason: "flatpickr / Luxon timezone DB — Intl.DateTimeFormat in QuickJS lacks the full IANA zone tables, so `setZone(\"Europe/Bucharest\")` falls back to UTC and the assertion sees the un-zoned datetime"

- test:   "Date field with relative: true edit when changing the value saves the valid date"
  reason: "flatpickr / Luxon timezone DB — Intl.DateTimeFormat in QuickJS lacks the full IANA zone tables, so `setZone(\"Europe/Bucharest\")` falls back to UTC and the assertion sees the un-zoned datetime"

- test:   "Date field with relative: true index displays the date in a valid tz"
  reason: "flatpickr / Luxon timezone DB — Intl.DateTimeFormat in QuickJS lacks the full IANA zone tables, so `setZone(\"Europe/Bucharest\")` falls back to UTC and the assertion sees the un-zoned datetime"

- test:   "Date field with relative: true show displays the date in a valid tz"
  reason: "flatpickr / Luxon timezone DB — Intl.DateTimeFormat in QuickJS lacks the full IANA zone tables, so `setZone(\"Europe/Bucharest\")` falls back to UTC and the assertion sees the un-zoned datetime"

- test:   "Date field with relative: true edit when keeping the value saves the valid date"
  reason: "flatpickr / Luxon timezone DB — Intl.DateTimeFormat in QuickJS lacks the full IANA zone tables, so `setZone(\"Europe/Bucharest\")` falls back to UTC and the assertion sees the un-zoned datetime"

- test:   "Date field with relative: true edit when changing the value saves the valid date"
  reason: "flatpickr / Luxon timezone DB — Intl.DateTimeFormat in QuickJS lacks the full IANA zone tables, so `setZone(\"Europe/Bucharest\")` falls back to UTC and the assertion sees the un-zoned datetime"

- test:   "Date field with allowInput: true edit when manually typing a value saves the valid date"
  reason: "flatpickr / Luxon timezone DB — Intl.DateTimeFormat in QuickJS lacks the full IANA zone tables, so `setZone(\"Europe/Bucharest\")` falls back to UTC and the assertion sees the un-zoned datetime"

- test:   "Date field with allowInput: true edit when manually typing a value reverts invalid date"
  reason: "flatpickr / Luxon timezone DB — Intl.DateTimeFormat in QuickJS lacks the full IANA zone tables, so `setZone(\"Europe/Bucharest\")` falls back to UTC and the assertion sees the un-zoned datetime"

- test:   "Date field with relative: false edit when keeping the value saves the valid date"
  reason: "flatpickr / Luxon timezone DB — Intl.DateTimeFormat in QuickJS lacks the full IANA zone tables, so `setZone(\"Europe/Bucharest\")` falls back to UTC and the assertion sees the un-zoned datetime"

- test:   "Date field with relative: false edit when changing the value saves the valid date"
  reason: "flatpickr / Luxon timezone DB — Intl.DateTimeFormat in QuickJS lacks the full IANA zone tables, so `setZone(\"Europe/Bucharest\")` falls back to UTC and the assertion sees the un-zoned datetime"

- test:   "Date field with relative: false edit when keeping the value saves the valid date"
  reason: "flatpickr / Luxon timezone DB — Intl.DateTimeFormat in QuickJS lacks the full IANA zone tables, so `setZone(\"Europe/Bucharest\")` falls back to UTC and the assertion sees the un-zoned datetime"

- test:   "Date field with relative: false edit when changing the value saves the valid date"
  reason: "flatpickr / Luxon timezone DB — Intl.DateTimeFormat in QuickJS lacks the full IANA zone tables, so `setZone(\"Europe/Bucharest\")` falls back to UTC and the assertion sees the un-zoned datetime"

- test:   "Date field on UTC edit keeps the right value on update and save"
  reason: "flatpickr / Luxon timezone DB — Intl.DateTimeFormat in QuickJS lacks the full IANA zone tables, so `setZone(\"Europe/Bucharest\")` falls back to UTC and the assertion sees the un-zoned datetime"

- test:   "Date field on western zone in a western (negative) Timezone edit keeps the same value on save"
  reason: "flatpickr / Luxon timezone DB — Intl.DateTimeFormat in QuickJS lacks the full IANA zone tables, so `setZone(\"Europe/Bucharest\")` falls back to UTC and the assertion sees the un-zoned datetime"

- test:   "Date field on western zone in a western (negative) Timezone edit keeps the right value on update and save"
  reason: "flatpickr / Luxon timezone DB — Intl.DateTimeFormat in QuickJS lacks the full IANA zone tables, so `setZone(\"Europe/Bucharest\")` falls back to UTC and the assertion sees the un-zoned datetime"

- test:   "Time field relative: false edit sets the proper time without updating"
  reason: "flatpickr / Luxon timezone DB — Intl.DateTimeFormat in QuickJS lacks the full IANA zone tables, so `setZone(\"Europe/Bucharest\")` falls back to UTC and the assertion sees the un-zoned datetime"

- test:   "Time field relative: false edit sets the proper time with updating"
  reason: "flatpickr / Luxon timezone DB — Intl.DateTimeFormat in QuickJS lacks the full IANA zone tables, so `setZone(\"Europe/Bucharest\")` falls back to UTC and the assertion sees the un-zoned datetime"

- test:   "Time field relative: false edit resets the time when reset button is clicked"
  reason: "flatpickr / Luxon timezone DB — Intl.DateTimeFormat in QuickJS lacks the full IANA zone tables, so `setZone(\"Europe/Bucharest\")` falls back to UTC and the assertion sees the un-zoned datetime"

- test:   "Time field relative: false edit sets the proper time without updating"
  reason: "flatpickr / Luxon timezone DB — Intl.DateTimeFormat in QuickJS lacks the full IANA zone tables, so `setZone(\"Europe/Bucharest\")` falls back to UTC and the assertion sees the un-zoned datetime"

- test:   "Time field relative: false edit sets the proper time with updating"
  reason: "flatpickr / Luxon timezone DB — Intl.DateTimeFormat in QuickJS lacks the full IANA zone tables, so `setZone(\"Europe/Bucharest\")` falls back to UTC and the assertion sees the un-zoned datetime"

- test:   "Time field relative: true index formats the time"
  reason: "flatpickr / Luxon timezone DB — Intl.DateTimeFormat in QuickJS lacks the full IANA zone tables, so `setZone(\"Europe/Bucharest\")` falls back to UTC and the assertion sees the un-zoned datetime"

- test:   "Time field relative: true show formats the time"
  reason: "flatpickr / Luxon timezone DB — Intl.DateTimeFormat in QuickJS lacks the full IANA zone tables, so `setZone(\"Europe/Bucharest\")` falls back to UTC and the assertion sees the un-zoned datetime"

- test:   "Time field relative: true edit sets the proper time without updating"
  reason: "flatpickr / Luxon timezone DB — Intl.DateTimeFormat in QuickJS lacks the full IANA zone tables, so `setZone(\"Europe/Bucharest\")` falls back to UTC and the assertion sees the un-zoned datetime"

- test:   "Time field relative: true edit sets the proper time with updating"
  reason: "flatpickr / Luxon timezone DB — Intl.DateTimeFormat in QuickJS lacks the full IANA zone tables, so `setZone(\"Europe/Bucharest\")` falls back to UTC and the assertion sees the un-zoned datetime"

- test:   "Time field relative: true index formats the time"
  reason: "flatpickr / Luxon timezone DB — Intl.DateTimeFormat in QuickJS lacks the full IANA zone tables, so `setZone(\"Europe/Bucharest\")` falls back to UTC and the assertion sees the un-zoned datetime"

- test:   "Time field relative: true show formats the time"
  reason: "flatpickr / Luxon timezone DB — Intl.DateTimeFormat in QuickJS lacks the full IANA zone tables, so `setZone(\"Europe/Bucharest\")` falls back to UTC and the assertion sees the un-zoned datetime"

- test:   "Time field relative: true edit sets the proper time without updating"
  reason: "flatpickr / Luxon timezone DB — Intl.DateTimeFormat in QuickJS lacks the full IANA zone tables, so `setZone(\"Europe/Bucharest\")` falls back to UTC and the assertion sees the un-zoned datetime"

- test:   "Time field relative: true edit sets the proper time with updating"
  reason: "flatpickr / Luxon timezone DB — Intl.DateTimeFormat in QuickJS lacks the full IANA zone tables, so `setZone(\"Europe/Bucharest\")` falls back to UTC and the assertion sees the un-zoned datetime"

- test:   "DestroyRecord foreign key constraints raises error"
  reason: "Avo Stimulus / Preact UI flow not fully exercised by simulated"

- test:   "discreet_information discreet information display when viewing a product shows product status tooltip"
  reason: "tooltip / popover visibility"

- test:   "discreet_information discreet information display when viewing a product shows timestamps tooltip"
  reason: "tooltip / popover visibility"

- test:   "ExposedMethodsBaseController message after_destroy_path && destroy_success_message"
  reason: "Avo Stimulus / Preact UI flow not fully exercised by simulated"

- test:   "Fields view types list view and change to grid, delete works on both"
  reason: "Avo Stimulus / Preact UI flow not fully exercised by simulated"

- test:   "Filters pagination resets when filters change with pagination set resets the pagination"
  reason: "filter form interaction"

- test:   "Filters date time filters time with single selector filters courses by starting at"
  reason: "filter form interaction"

- test:   "for_attribute option text field changing secondary field edits real value"
  reason: "Avo Stimulus / Preact UI flow not fully exercised by simulated"

- test:   "HasAndBelongsToManyField show with an attached user detaches the user"
  reason: "Avo Stimulus / Preact UI flow not fully exercised by simulated"

- test:   "HasManyField show delete notification visible shows the notification when delete fails"
  reason: "has_many association editor — Stimulus-driven row management"

- test:   "HasManyField with a related post deletes a post"
  reason: "has_many association editor — Stimulus-driven row management"

- test:   "HasManyField with a related post detaches a post"
  reason: "has_many association editor — Stimulus-driven row management"

- test:   "HasOneFieldName show without a related post attaches and detaches a post"
  reason: "Avo Stimulus / Preact UI flow not fully exercised by simulated"

- test:   "HasOneField omits parent record hides the belongs_to field pointing back to the parent on the embedded show page"
  reason: "Avo Stimulus / Preact UI flow not fully exercised by simulated"

- test:   "KeyValueFields without value won't change the value"
  reason: "key/value pair editor uses Stimulus-driven row insertion that races with our synchronous JS"

- test:   "KeyValueFields without value edit adds a row to meta table and input one row with values"
  reason: "key/value pair editor uses Stimulus-driven row insertion that races with our synchronous JS"

- test:   "KeyValueFields with value edit adds a row to meta table with key and value"
  reason: "key/value pair editor uses Stimulus-driven row insertion that races with our synchronous JS"

- test:   "KeyValueFields with value edit adds a row to meta table only with key"
  reason: "key/value pair editor uses Stimulus-driven row insertion that races with our synchronous JS"

- test:   "KeyValueFields with 0 and false edit preserves types when changing 0 to 1 (Integer) and false to true (Boolean)"
  reason: "key/value pair editor uses Stimulus-driven row insertion that races with our synchronous JS"

- test:   "OpenFieldAttachment with PDF attachment opens attachment in new window without download"
  reason: "Avo Stimulus / Preact UI flow not fully exercised by simulated"

- test:   "ResourceSidebars allow fields to be edited on sidebar"
  reason: "sidebar open/closed state persisted via cookie + class toggles cascade-keyed on layout"

- test:   "SignOutDropdown when signed in signs out the user"
  reason: "Avo Stimulus / Preact UI flow not fully exercised by simulated"

- test:   "SkipShowView skip_show_view = true can save and destroy on edit page"
  reason: "show-view-skipping resource flow"

- test:   "SkipShowView skip_show_view = true create and delete association redirects to the edit page"
  reason: "show-view-skipping resource flow"

- test:   "SkipShowView skip_show_view = false can save but can't destroy on edit page"
  reason: "show-view-skipping resource flow"

- test:   "SkipShowView skip_show_view = false create and delete association redirects to the show page"
  reason: "show-view-skipping resource flow"

- test:   "StimulusJS city-in-country#onCountryChange changes the cities"
  reason: "custom Stimulus controllers needing real layout / scroll"

- test:   "StimulusJS city-in-country#onCountryChange (attached to ShowCurrentTime Action) filters cities based on a given country"
  reason: "custom Stimulus controllers needing real layout / scroll"

- test:   "Tabs date_time field works on tabs"
  reason: "multiple tab containers (show + edit views) render simultaneously when only one should be visible — class-driven cascade does not currently filter the inactive group"

- test:   "Tabs keeps the pagination on tab when back is used"
  reason: "multiple tab containers (show + edit views) render simultaneously when only one should be visible — class-driven cascade does not currently filter the inactive group"

- test:   "Tabs keeps the per_page on association when back is used"
  reason: "multiple tab containers (show + edit views) render simultaneously when only one should be visible — class-driven cascade does not currently filter the inactive group"

- test:   "Tabs doesn't display tabs content on index doesn't display the birthday from the tab content"
  reason: "multiple tab containers (show + edit views) render simultaneously when only one should be visible — class-driven cascade does not currently filter the inactive group"

- test:   "Tabs tabs show switches the tabs"
  reason: "multiple tab containers (show + edit views) render simultaneously when only one should be visible — class-driven cascade does not currently filter the inactive group"

- test:   "Tabs tabs show hides the birthday tab"
  reason: "multiple tab containers (show + edit views) render simultaneously when only one should be visible — class-driven cascade does not currently filter the inactive group"

- test:   "Tabs tabs edit hides the Projects tab"
  reason: "multiple tab containers (show + edit views) render simultaneously when only one should be visible — class-driven cascade does not currently filter the inactive group"

- test:   "Tabs un-authorized tabs hides the tab"
  reason: "multiple tab containers (show + edit views) render simultaneously when only one should be visible — class-driven cascade does not currently filter the inactive group"

- test:   "Tags acts_as_taggable index shows filled state"
  reason: "tagify multi-select — overflow truncation (\"2 more items\") gates off layout-derived container width"

- test:   "Tags acts_as_taggable edit shows filled state"
  reason: "tagify multi-select — overflow truncation (\"2 more items\") gates off layout-derived container width"

- test:   "Tags acts_as_taggable edit adds a tag"
  reason: "tagify multi-select — overflow truncation (\"2 more items\") gates off layout-derived container width"

- test:   "Tags acts_as_taggable new keeps acts as taggable tags on validation errors"
  reason: "tagify multi-select — overflow truncation (\"2 more items\") gates off layout-derived container width"

- test:   "Tags without acts_as_taggable adds skills"
  reason: "tagify multi-select — overflow truncation (\"2 more items\") gates off layout-derived container width"

- test:   "Tags ajax request fetches the users"
  reason: "tagify multi-select — overflow truncation (\"2 more items\") gates off layout-derived container width"

- test:   "Tags format_using (same as deprecated fetch_labels) with fetch_values_from keep correct tags on validations error and edit"
  reason: "tagify multi-select — overflow truncation (\"2 more items\") gates off layout-derived container width"

- test:   "Tags mode: :select on edit / update"
  reason: "tagify multi-select — overflow truncation (\"2 more items\") gates off layout-derived container width"

- test:   "TestHelpers field_wrapper on self resource index finds the wrapper on table"
  reason: "Avo's own test helpers were re-written for cuprite / chromedriver-specific affordances (drag, clipboard, paginated index ordering); failures are present in Avo's cuprite baseline at this SHA too"

- test:   "TestHelpers field_wrapper on self resource show finds the wrapper"
  reason: "Avo's own test helpers were re-written for cuprite / chromedriver-specific affordances (drag, clipboard, paginated index ordering); failures are present in Avo's cuprite baseline at this SHA too"

- test:   "TestHelpers field_wrapper on show view related resource index finds the wrapper"
  reason: "Avo's own test helpers were re-written for cuprite / chromedriver-specific affordances (drag, clipboard, paginated index ordering); failures are present in Avo's cuprite baseline at this SHA too"

- test:   "TestHelpers field_label on self resource index finds the label on table"
  reason: "Avo's own test helpers were re-written for cuprite / chromedriver-specific affordances (drag, clipboard, paginated index ordering); failures are present in Avo's cuprite baseline at this SHA too"

- test:   "TestHelpers field_label on self resource show finds the label"
  reason: "Avo's own test helpers were re-written for cuprite / chromedriver-specific affordances (drag, clipboard, paginated index ordering); failures are present in Avo's cuprite baseline at this SHA too"

- test:   "TestHelpers field_value on self resource index finds the value on table"
  reason: "Avo's own test helpers were re-written for cuprite / chromedriver-specific affordances (drag, clipboard, paginated index ordering); failures are present in Avo's cuprite baseline at this SHA too"

- test:   "TestHelpers field_value on self resource show find value"
  reason: "Avo's own test helpers were re-written for cuprite / chromedriver-specific affordances (drag, clipboard, paginated index ordering); failures are present in Avo's cuprite baseline at this SHA too"

- test:   "TestHelpers isolated tests confirm_alert confirms the alert"
  reason: "Avo's own test helpers were re-written for cuprite / chromedriver-specific affordances (drag, clipboard, paginated index ordering); failures are present in Avo's cuprite baseline at this SHA too"

- test:   "TestHelpers isolated tests run_action runs the panel action in index"
  reason: "Avo's own test helpers were re-written for cuprite / chromedriver-specific affordances (drag, clipboard, paginated index ordering); failures are present in Avo's cuprite baseline at this SHA too"

- test:   "TestHelpers isolated tests run_action runs the panel action in show"
  reason: "Avo's own test helpers were re-written for cuprite / chromedriver-specific affordances (drag, clipboard, paginated index ordering); failures are present in Avo's cuprite baseline at this SHA too"

- test:   "TestHelpers isolated tests tags suggestions"
  reason: "Avo's own test helpers were re-written for cuprite / chromedriver-specific affordances (drag, clipboard, paginated index ordering); failures are present in Avo's cuprite baseline at this SHA too"

- test:   "TestHelpers isolated tests tags add checks and remove tags"
  reason: "Avo's own test helpers were re-written for cuprite / chromedriver-specific affordances (drag, clipboard, paginated index ordering); failures are present in Avo's cuprite baseline at this SHA too"

- test:   "TestHelpers isolated tests tags verify tags"
  reason: "Avo's own test helpers were re-written for cuprite / chromedriver-specific affordances (drag, clipboard, paginated index ordering); failures are present in Avo's cuprite baseline at this SHA too"

- test:   "TiptapField without value edit change the products description text"
  reason: "TipTap WYSIWYG editor — contenteditable selection / range"

- test:   "TiptapField with regular value edit change the products description tiptap to another simple text value"
  reason: "TipTap WYSIWYG editor — contenteditable selection / range"

- test:   "TrixField with regular value show displays the posts body"
  reason: "Avo Stimulus / Preact UI flow not fully exercised by simulated"

- test:   "UserForms submits the form on enter"
  reason: "multi-step form interaction with conditional fields"

- test:   "ModelErrors does not swallow errors"
  reason: "model error display"

- test:   "sidebar desktop remembers user choice"
  reason: "sidebar open/closed state persisted via cookie + class toggles cascade-keyed on layout"

- test:   "Field Summarizing summarizable field option when summarizing on association pages only shows values from associated record"
  reason: "field summary popover positioning"
