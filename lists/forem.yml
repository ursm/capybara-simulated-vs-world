# Tests we don't expect capybara-simulated to satisfy. Each entry is
# `{test:, reason:}`; `test:` is either an exact RSpec full_description
# or `./spec/...rb:LINE` location string, or a Regexp matched against
# either.
#
# Last verified against Forem 99141cc1f on 2026-05-08
# (capybara-simulated 0.0.7 / Ruby 3.3.11), `bin/run-forem`'s default
# `--tag js spec/system` selector.

- test: "./spec/system/notifications/notifications_page_spec.rb:100"
  reason: "ahoy tracking event is fired from a Preact effect that races with our synchronous JS execution; the assertion runs before `Ahoy::Event.count` reaches 1"
